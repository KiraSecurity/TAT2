import os
import subprocess

def chainer(lib):
    #chain =[]
    
    # for key in lib.keys():
    
    #     if key in categories:
    #         chain.append(key)
    #         print(chain)
    return chain_lib